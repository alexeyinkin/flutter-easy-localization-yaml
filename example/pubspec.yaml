name: example
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.1.3 <4.0.0'

dependencies:
  easy_localization: ^3.0.3
  easy_localization_yaml: { path: ../ }
  flutter: { sdk: flutter }

dev_dependencies:
  total_lints: ^3.1.1

flutter:
  assets:
    - assets/translations/en.yaml
